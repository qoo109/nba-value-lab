# Kaggle Closing Benchmark Runbook

Use the `Build Kaggle closing market benchmark` workflow with the default dataset handle and walk-forward source run. Review the uploaded report artifact only; raw Kaggle and normalized game-level files must remain temporary.
