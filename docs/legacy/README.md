# Legacy Frontend Files

這裡保存不再由目前 `index.html` 載入、但仍值得留作歷史參考的舊前端檔案。

- `dual-engine-preview.html`：早期 V3 x G1 雙引擎靜態預覽。
- `script-v4.js`：模組化前的 V4 單檔 JavaScript。

這些檔案不是目前 GitHub Pages 主站入口，也不代表最新模型、資料或研究狀態。
